async function getSession(sessionId) {}

async function regenerateSession() {}

async function getTime() {
  const currentDate = new Date();

  // do logic to return seconds/minutes/hours/days(?) to finish the mission
}
